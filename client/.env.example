# API base URL (no trailing slash) — must match your Express server (default port 3000).
VITE_API_URL=http://localhost:8080

# Required for POST /auth/login (tenant slug in your database).
VITE_TENANT_SLUG=nvm-school

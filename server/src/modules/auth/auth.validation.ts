/**
 * Auth module — add Joi schemas here (e.g. signup, login, invite bodies) and wire them
 * in `routes/auth.routes.ts` with `validate({ body: ... })` from
 * `middleware/validate.middleware.ts`.
 *
 * Keeping validation in this file matches the student module pattern and avoids
 * duplicating rules inside controllers.
 */

export {};
